# Test coverage
coverage/

# Dependencies
node_modules/

# Build output
dist/

# Logs
*.log
logs/

# OS
.DS_Store

# IDE
.vscode/
.idea/

# Git worktrees
.worktrees/

.claude/scheduled_tasks.lock

# Phase 5 embed assets — large native binaries staged at build time (CI/goreleaser
# or scripts/stage-runtime-assets.sh). Gitignored like the PoC's embedded/ dir;
# only a .gitkeep is committed so //go:embed has a directory to point at.
internal/core/runtime/embedded/libonnxruntime.dylib
internal/core/runtime/embedded/libonnxruntime.so
internal/core/runtime/embedded/tokenizer.json

# Go binaries (built locally via scripts/build-binaries.sh)
/bin/memmem
/bin/memmem-mcp

# Local dev build assets (fetched by scripts/fetch-dev-assets.sh)
/poc/lib/
/.cache/
